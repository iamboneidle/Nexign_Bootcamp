spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${BRT_DB_URL}
spring.datasource.username=${BRT_DB_USERNAME}
spring.datasource.password=${BRT_DB_PASSWORD}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

spring.data.redis.host=${BRT_REDIS_HOST}
spring.data.redis.port=${BRT_REDIS_PORT}

spring.kafka.bootstrap-servers=${KAFKA_URL}

crm.service.url.post-tariffs=${POST_TARIFFS_TO_CRM_URL}
crm.service.url.change-tariff=${CHANGE_TARIFF_URL}
crm.service.url.put-money=${PUT_MONEY_URL}
crm.admin.username=${ADMIN_USERNAME}
crm.admin.password=${ADMIN_PASSWORD}


hrs.service.post-data-to-pay=${POST_DATA_TO_PAY_URL}
hrs.service.url.post-rate-data=${POST_RATES_TO_HRS_REDIS}

server.port=${BRT_SERVER_PORT}

spring.devtools.restart.enabled=false
